{
  "opsVersion": 1,
  "operations": [
    {
      "name": "health",
      "description": "Liveness check (PocketBase built-in).",
      "system": true,
      "params": {},
      "executor": { "type": "http", "method": "GET", "path": "/api/health" }
    },
    {
      "name": "ops.list",
      "description": "Return this operations manifest.",
      "system": true,
      "params": {},
      "executor": { "type": "http", "method": "GET", "path": "/api/_ops" }
    },
    {
      "name": "invoices.simulate-bill",
      "description": "Simulate an incoming email invoice/bill and process its line items and subscription link.",
      "params": {
        "preset": {
          "type": "string",
          "description": "Preset to simulate (aws, openai, or figma)",
          "default": "aws"
        }
      },
      "executor": { "type": "http", "method": "POST", "path": "/api/ops/invoices/simulate-bill" }
    }
  ]
}
